## Изменение

В файле `src/components/FloatingContactButton.tsx` уменьшить главную плавающую кнопку с 80×80 px (`w-20 h-20`) до 60×60 px (`w-[60px] h-[60px]`).

Также пропорционально уменьшить иконки внутри кнопки (Phone и X) с `w-9 h-9` (36px) до `w-7 h-7` (28px), чтобы сохранить визуальный баланс.

Кнопки в развёрнутом меню (WhatsApp, Telegram, VK, Позвонить) остаются прежнего размера `w-14 h-14`.