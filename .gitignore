# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local env files (Supabase URL/publishable key are non-secret but should
# still come from the hosting platform's Environment Variables, not git).
.env
.env.*
!.env.example

# Проект использует bun.lock; package-lock.json появляется только от
# локального `npm install` и в репозиторий не нужен.
package-lock.json
